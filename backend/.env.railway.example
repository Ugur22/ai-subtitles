# Railway Environment Variables Template
# Copy these to Railway's Variables section in your service settings

# ===========================================
# REQUIRED: API Keys
# ===========================================
HUGGINGFACE_TOKEN=hf_xxx
OPENAI_API_KEY=sk-xxx
GROQ_API_KEY=gsk_xxx

# ===========================================
# ML Features (Disabled for Railway CPU)
# ===========================================
# These will be handled by Modal for GPU processing
ENABLE_SPEAKER_DIARIZATION=false
ENABLE_VISUAL_SEARCH=false
ENABLE_AUDIO_ANALYSIS=false
ENABLE_SPEECH_EMOTION=false
VAD_ENABLED=false

# ===========================================
# Whisper Configuration (if running on Railway)
# ===========================================
FASTWHISPER_MODEL=small
FASTWHISPER_DEVICE=cpu
FASTWHISPER_COMPUTE_TYPE=int8

# ===========================================
# LLM Configuration
# ===========================================
DEFAULT_LLM_PROVIDER=groq
GROQ_MODEL=llama-3.1-70b-versatile

# ===========================================
# CORS Configuration
# ===========================================
# Update with your Vercel frontend URL after deployment
# Format must be a JSON array
CORS_ORIGINS=["https://your-app.vercel.app","http://localhost:5173"]

# ===========================================
# Persistent Storage Paths (Railway Volumes)
# ===========================================
# These paths should match your Railway volume mount points
DATABASE_PATH=/data/db/transcriptions.db
CHROMA_DB_PATH=/data/chroma_db
VIDEOS_DIR=/data/static/videos
SCREENSHOTS_DIR=/data/static/screenshots
STATIC_DIR=/data/static

# ===========================================
# Optional: Additional LLM Providers
# ===========================================
# ANTHROPIC_API_KEY=sk-ant-xxx
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022
# XAI_API_KEY=xxx
# XAI_MODEL=grok-beta
