# Local mode reference — you normally do NOT need this file:
# scripts/run-local.sh exports these for the Vite process, overriding frontend/.env.
VITE_API_URL=http://localhost:8000
VITE_LOCAL_MODE=true
# No Supabase in local mode: job progress uses polling instead of realtime.
